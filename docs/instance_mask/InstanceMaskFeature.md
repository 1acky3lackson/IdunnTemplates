# 实例蒙版

用途：主要是为了让玩家能放置一个模板的一部分，但同时，蒙版后的实例仍然是个长方体区域。因此需要在实例中添加属性

* X-方向的缩进长度
* X+方向的缩进长度
* Y+方向的缩进长度
* Y+方向的缩进长度
* Z+方向的缩进长度
* Z+方向的缩进长度

这些数字默认是0，可以提供正整数。让最终实例生效的部分比模板的原始大小相等或者小一圈，于是我们可以放置模板的一部分。

同时，模板旧有关于坐标和计算的方法不变。只是在放置和更新的时候要忽略去除掉的部分。